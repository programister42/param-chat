import { InvalidPopoverDirective } from './invalid-popover.directive';

describe('InvalidPopoverDirective', () => {
	it('should create an instance', () => {
		const directive = new InvalidPopoverDirective();
		expect(directive).toBeTruthy();
	});
});
